import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "Mounika",
  initialMessages: [
    createChatBotMessage("Hello! 👋 I’m MounikaBot. How can I help you today?"),
  ],
};

export default config;